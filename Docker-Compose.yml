version: '3.8'

volumes:
  db:
    driver: local

networks:
  freeport:
    external: true

  api:
    container_name: freeport-background
    image: freeport/freeport-background
    restart: on-failure
    build:
        context: ./
        dockerfile: Dockerfile
    volumes:
        - ./:/app
    ports:
      - '6020:6020'
    networks:
      - freeport
